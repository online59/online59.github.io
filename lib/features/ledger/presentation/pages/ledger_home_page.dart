import 'package:flutter/material.dart';

class LedgerHomePage extends StatelessWidget {
  const LedgerHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return const SingleChildScrollView(
      child: Column(
        children: [
          
        ],
      ),
    );
  }
}